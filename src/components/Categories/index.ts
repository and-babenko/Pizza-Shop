import Categories from "./Categories";
import { categoryIdType, sortByType, sortPropertyType } from "./types";

export default Categories;
export type { categoryIdType, sortByType, sortPropertyType };
